void setup() {
  Serial.begin(9600);  // 시리얼 통신 시작
}

void loop() {
  // if (Serial.available() > 0) {  // 데이터가 있는지 확인
  //   String data = Serial.readStringUntil('\n');  // 데이터를 읽고 문자열로 변환
  //   Serial.print("Received: ");  // 데이터 출력
  //   Serial.println(data);
  // }
}
