<UserControl x:Class="Monitoring.Views.ProcessMonitoring"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Monitoring.Views"
      xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="77"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <!-- 작업 DataGrid 영역 -->
        <StackPanel Grid.RowSpan="3">
            <DataGrid x:Name="DrdWorkStatus" AutoGenerateColumns="False" Margin="10" IsReadOnly="True" HorizontalAlignment="Left" 
                      VerticalAlignment="Top" GridLinesVisibility="All" BorderBrush="Gray" BorderThickness="1" 
                      IsSynchronizedWithCurrentItem="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding WorkNum}" Header="작업번호" FontWeight="Bold"/>
                    <DataGridTextColumn Binding="{Binding OrderNum}" Header="주문번호" FontWeight="Bold"/>
                    <DataGridTextColumn Binding="{Binding DeliveryNum}" Header="배송번호" FontWeight="Bold"/>
                    <DataGridTextColumn Binding="{Binding ProcessDT}" Width="100" Header="작업일시" FontWeight="Bold"/>
                    <DataGridTextColumn Binding="{Binding CompleteOrNot}" Header="성공여부" FontWeight="Bold"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
        <!-- 작업 현황 차트 영역 -->
        <Grid Grid.Row="0" Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <lvc:PieChart x:Name="ChtProduct" Grid.Row="0"/>
            <lvc:PieChart x:Name="ChtDefinition"  Series="{Binding DefinitionSeries}" Grid.Row="0"/>
        </Grid>
        
        <!-- 온습도 차트 영역 -->
        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Label x:Name="LblSensingDt" Content="2023-07-06 22:00:00" FontSize="14" 
                   VerticalAlignment="Center" Margin="20,0" Grid.Row="0"/>
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Grid Grid.Column="0">
                    <lvc:PieChart x:Name="ChtTemp" MinValue="0" MaxValue="100" MaxAngle="270" InitialRotation="-225" Margin="10"></lvc:PieChart>
                    </Grid>
                    <Grid Grid.Column="1">
                    <lvc:PieChart x:Name="ChtHumid" MinValue="0" MaxValue="100" MaxAngle="270" InitialRotation="-225" Margin="10"></lvc:PieChart>
                    </Grid>
                </Grid>
                <!--<Label Content="현재 온도 :" VerticalAlignment="Center" FontFamily="NanumGothic" FontSize="20" Margin="10,0,0,0" FontWeight="Bold"/>
                <TextBox x:Name="TbxTemp" HorizontalAlignment="Right" Margin="5,5" Height="40" Width="70" FontSize="20" FontWeight="DemiBold" BorderBrush="White" IsReadOnly="True"/>
                <Label Content="%" VerticalAlignment="Center" FontFamily="NanumGothic" FontSize="20" Margin="5,0,0,0" FontWeight="Bold" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Content="현재 습도 :" VerticalAlignment="Center" FontFamily="NanumGothic" FontSize="20" Margin="10,0,0,0" FontWeight="Bold" />
                <TextBox x:Name="TbxHumid" HorizontalAlignment="Right" Margin="5,5" Height="40" Width="70" FontSize="20" FontWeight="DemiBold" BorderBrush="White" IsReadOnly="True"/>
                <Label Content="°C" VerticalAlignment="Center" FontFamily="NanumGothic" FontSize="20" Margin="5,0,0,0" FontWeight="Bold" />-->
        </Grid>
        <StackPanel Grid.Row="2" Grid.Column="1" VerticalAlignment="Bottom">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="BtnStart" Content="Start" FontSize="15" Margin="10" Width="150" Height="50" Click="BtnStart_Click" FontWeight="DemiBold"/>
                <Button x:Name="BtnStop" Content="Stop" FontSize="15" Margin="10" Width="150" Click="BtnStop_Click" FontWeight="DemiBold"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
