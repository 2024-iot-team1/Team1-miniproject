<UserControl x:Class="Monitoring.Views.Order"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Monitoring.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800" >

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="5" Grid.ColumnSpan="2">
            <DataGrid x:Name="OrderDataGrid" Grid.Column="0" AutoGenerateColumns="False" Margin="10,10,0,0"
                      HorizontalAlignment="Left" VerticalAlignment="Top"
                      Width="336" Height="390" GridLinesVisibility="All"
                      BorderBrush="Gray" BorderThickness="1" IsSynchronizedWithCurrentItem="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="주문 번호" Binding="{Binding OrderNum}" FontWeight="Bold"  />
                    <DataGridTextColumn Header="제품 번호" Binding="{Binding InventoryNum}" FontWeight="Bold" />
                    <DataGridTextColumn Header="수량" Binding="{Binding Quantity}" FontWeight="Bold" />
                    <DataGridTextColumn Header="주문 날짜" Binding="{Binding OrderDT}" FontWeight="Bold" />
                </DataGrid.Columns>
                <!--<DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderBrush" Value="LightGray" />
                        <Setter Property="BorderThickness" Value="1" />
                        <Setter Property="Padding" Value="5" />
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="BorderBrush" Value="Gray" />
                        <Setter Property="BorderThickness" Value="0,0,1,1" />
                        <Setter Property="HorizontalContentAlignment" Value="Center" />
                        <Setter Property="Padding" Value="5" />
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="BorderBrush" Value="Gray" />
                        <Setter Property="BorderThickness" Value="0,0,0,1" />
                    </Style>
                </DataGrid.RowStyle>-->
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
